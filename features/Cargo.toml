[package]
name = "features"
version = "0.1.0"
edition = "2021"
publish = false
description = "A virtual crate that helps declare macros with complex features."

[features]
default = ["std"]
std = []

[lib]
path = "src/lib.rs"
