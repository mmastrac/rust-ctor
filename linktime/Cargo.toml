[package]
name = "linktime"
version = "0.19.0"
authors.workspace = true
edition = "2021"
description = "Link-time registration patterns for Rust (ctors, dtors, link-time slices and sections)"
license.workspace = true
repository.workspace = true
categories = ["development-tools::build-utils", "no-std", "no-std::no-alloc", "embedded", "development-tools::procedural-macro-helpers"]
rust-version = "1.60"

[features]
default = ["std", "proc_macro", "ctor", "dtor", "link-section"]

ctor = ["dep:ctor"]
dtor = ["dep:dtor"]
link-section = ["dep:link-section"]

std = ["ctor?/std", "dtor?/std", "link-section?/std"]

# Enable the proc_macro
proc_macro = ["dep:linktime-proc-macro", "ctor?/proc_macro", "dtor?/proc_macro", "link-section?/proc_macro"]

[dependencies]
linktime-proc-macro = { workspace = true, optional = true }
ctor = { workspace = true, optional = true }
dtor = { workspace = true, optional = true }
link-section = { workspace = true, optional = true }

# UEFI needs the constructor-collection machinery behind `ctor/priority`. Other
# targets keep the default naked `.init_array` placement.
[target.'cfg(target_os = "uefi")'.dependencies]
ctor = { workspace = true, optional = true, features = ["priority"] }

[dev-dependencies]
libc-print = "0.5"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = [
    'cfg(linktime_no_fail_on_missing_unsafe)',
    'cfg(linktime_used_linker)'
] }
