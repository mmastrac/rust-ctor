[package]
name = "tests"
version = "0.1.0"
edition = "2021"
publish = false

[dev-dependencies]
clitest-lib = { version = "0.4.7", default-features = false, features = ["fancy-regex"] }

[lib]
path = "test.rs"
