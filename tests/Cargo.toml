[package]
name = "tests"
version = "0.1.0"
edition = "2021"
publish = false

[dev-dependencies]
clitest-lib = "0.4.4"
# Avoid bringing in a native C dependency in onig
grok = { version = "2", features = ["fancy-regex"] }

[lib]
path = "test.rs"
